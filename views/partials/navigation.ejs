<nav>
    <%- nav %> <!-- This EJS tag is used to include the navigation content generated by the "getNav" function from the utilities module. The "nav" variable is passed to the view when rendering, and it contains the HTML for the navigation links based on the classifications retrieved from the database. By including this tag in the navigation section of the layout, it allows for dynamic generation of navigation links that can be easily updated as classifications are added or removed from the database. -->
</nav>

<%# For a reminder on what EJS tags do, see the comments in the layout.ejs file. %>